# wrangler local state and secret files (infra/turn-worker)
.wrangler/
.dev.vars
*.secrets.json

# build and test output. dist/ is anchored to the root: test/dist/ holds the dist guards.
/dist/
node_modules
coverage/
playwright-report/
test-results/
*.tsbuildinfo
# written by playwright.config.ts per run: the TURN ICE list (its port follows E2E_PORT_OFFSET)
# and coturn's pidfile
e2e/fixtures/.generated/

# personal Claude Code permissions for this checkout
.claude/settings.local.json
