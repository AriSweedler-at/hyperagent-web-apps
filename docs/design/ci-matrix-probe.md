# CI matrix probe

A docs-only diff against dry-d9-ci-matrix: the changes job must emit `games=[]` and `e2e-games=[]`, both matrix jobs must skip whole and ci-ok must stay green (dry-round-2.md I1). This file and its PR are deleted once the run is read.
