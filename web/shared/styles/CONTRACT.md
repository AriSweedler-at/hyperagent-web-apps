# CSS <-> TS class contract

Every class a game's TypeScript names has a rule in the stylesheet its page links, and every class
that stylesheet styles is named by TypeScript or carried by the page markup.
`test/dist/class-contract.test.ts` (docs/MIGRATION.md step 14; from `npm run test:site`, after the
build) enforces both directions on dist/ for each game, reading three sources through
`test/dist/classes.ts`:

- TS-named: the quoted names passed to `toggleClass`/`addClass`/`removeClass`/`hasClass`
  (`web/shared/edge/dom.ts`), the fidice vdom's `class:` props and the quoted arguments of its
  `cls(...)`, and the words of every `class="..."` attribute in a template string (plus the quoted
  words of a `${...}` ternary inside it), over `web/games/<g>/**` and `web/shared/**` (`.ts`, not
  tests).
- Markup: the `class="..."` attributes of the served `games/<g>/index.html`.
- CSS: every `.name` in the stylesheets the page links (the shared chunk's
  `dist/shared/assets/<chunk>-<hash>.css`, which is `web/shared/styles/{tokens,base}.css`, then
  `dist/shared/assets/<g>-<hash>.css`, its theme), with comments and strings blanked first.

The extraction is a few documented regular expressions, not a parser. Everything it cannot see is a
row below, and the test checks each row against the tree so a row cannot go stale (a class whose
rule is gone, a "dead" rule TS started naming). Adding a screen or a class means keeping this table
true (docs/ARCHITECTURE.md "Conventions for small diffs"); a class named literally and styled needs
no row.

| Owner      | Kind  | Name                                                                             | Toggled by (TS)                                                                                                                                                                                                 | Styled in (CSS)                  | Notes                                                                                                                                                                                                                                                                                                          |
| ---------- | ----- | -------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| gin-rummy  | class | `card red mini big selected dim fresh locked`                                    | `web/games/gin-rummy/src/ui/cards.ts` (`cardClass`)                                                                                                                                                             | `web/games/gin-rummy/theme.css`  | Built from `CardOptions`; the extraction sees `card`, `big`, `dim` elsewhere and none of the rest.                                                                                                                                                                                                             |
| gin-rummy  | class | `black`                                                                          | `web/games/gin-rummy/src/ui/cards.ts` (`cardClass`)                                                                                                                                                             |                                  | The default card colour: only `.card.red` has a rule.                                                                                                                                                                                                                                                          |
| gin-rummy  | class | `tiny`                                                                           | `web/games/gin-rummy/src/ui/render.ts` (`backHtml('tiny')`)                                                                                                                                                     | `web/games/gin-rummy/theme.css`  | Passed as `backHtml`'s argument, not in a `class="..."`.                                                                                                                                                                                                                                                       |
| gin-rummy  | class | `m0 m1 m2 m3 m4`                                                                 | `web/games/gin-rummy/src/ui/hand/meldGroups.ts` (`meldGroupClass`), `src/ui/hand/SlotHandView.ts`                                                                                                               | `web/games/gin-rummy/theme.css`  | Template `m${index % 5}` (`.meld-group.mN` for the sheets, `.slot.mN` for the hand).                                                                                                                                                                                                                           |
| gin-rummy  | class | `on off`                                                                         | `web/games/gin-rummy/src/ui/render.ts` (`connDotClass`)                                                                                                                                                         | `web/games/gin-rummy/theme.css`  | `#connDot`'s whole class attribute is a template written with `setAttr`.                                                                                                                                                                                                                                       |
| gin-rummy  | class | `group n1 n2 n3 n4 n5 n6 n7 n8 n9 n10 n11`                                       | `web/games/gin-rummy/src/ui/hand/SlotHandView.ts` (`groupClass`)                                                                                                                                                | `web/games/gin-rummy/theme.css`  | Template `group n${len}`: a meld's grid item spans its length (`.group.nK`).                                                                                                                                                                                                                                   |
| gin-rummy  | class | `dec inc`                                                                        | `web/games/gin-rummy/src/scorer/main.ts`                                                                                                                                                                        |                                  | Click hooks on the stepper's buttons; styled as `.stepper button`.                                                                                                                                                                                                                                             |
| gin-rummy  | class | `history-meta`                                                                   | `web/games/gin-rummy/src/ui/render.ts`, `src/scorer/main.ts`                                                                                                                                                    |                                  | Layout wrapper of a history row; no rule (legacy markup, kept for DOM parity).                                                                                                                                                                                                                                 |
| gin-rummy  | class | `divider`                                                                        |                                                                                                                                                                                                                 | `web/games/gin-rummy/theme.css`  | Nothing carries it: the legacy home screen's "or" rule. Dead, removal is step 15's.                                                                                                                                                                                                                            |
| gin-rummy  | class | `jargon rule-flash`                                                              | `web/shared/ui/glossary.ts` (`JARGON_CLASS`), `web/shared/edge/glossary.ts` (`RULE_FLASH_CLASS`)                                                                                                                | `web/games/gin-rummy/theme.css`  | Glossary links (docs/design/glossary-links.md): the link's class is a `${}` hole in the template, the flash a constant passed to `addClass`, so the extraction sees neither.                                                                                                                                   |
| gin-rummy  | class | `pinned`                                                                         | `web/games/gin-rummy/src/ui/render.ts` (`paintTableMelds`, the card's `extra` option)                                                                                                                           | `web/games/gin-rummy/theme.css`  | The knocker's cards on the table while a knock is answered (docs/design/gin-arrangement-and-discards.md §7b): a steel edge, locked in place. `laid` beside it is a quoted ternary word the extraction reads.                                                                                                   |
| fidice     | class | `abs-blank abs-kayak abs-pine abs-step1 abs-step2 abs-step3 abs-step4 abs-step5` | `web/games/fidice/src/view/components.ts` (`shapeDie`)                                                                                                                                                          | `web/games/fidice/theme.css`     | Template `abs-${sym}` over `ShapeSym`.                                                                                                                                                                                                                                                                         |
| fidice     | class | `sm xs`                                                                          | `web/games/fidice/src/view/components.ts` (`die`, `size`)                                                                                                                                                       | `web/games/fidice/theme.css`     | Die sizes arrive as the `size` argument (`diceRow(dice, 'xs')`, `shapeDie(sym, 'sm')`).                                                                                                                                                                                                                        |
| fidice     | class | `app`                                                                            | `web/games/fidice/src/view/app.ts`                                                                                                                                                                              |                                  | The root's class; the rules use `#app-root`.                                                                                                                                                                                                                                                                   |
| fidice     | class | `shape`                                                                          | `web/games/fidice/src/view/components.ts` (`shapeRow`)                                                                                                                                                          |                                  | `.dice.shape` marks a shape row; no rule.                                                                                                                                                                                                                                                                      |
| fidice     | class | `remove-bot`                                                                     | `web/games/fidice/src/view/components.ts`                                                                                                                                                                       |                                  | Marks the lobby's remove button beside `btn-ghost small`; no rule.                                                                                                                                                                                                                                             |
| fidice     | class | `lobby`                                                                          | `web/games/fidice/src/view/components.ts`                                                                                                                                                                       |                                  | False positive: the phase literal in `game.phase !== 'lobby' && 'out'` inside a `cls(...)`. Not a class.                                                                                                                                                                                                       |
| fidice     | class | `blurb cat`                                                                      |                                                                                                                                                                                                                 | `web/games/fidice/theme.css`     | `.ladder .cat .blurb`: the legacy ladder's category rows; the typed ladder builds `catrow`. Dead, removal is step 15's.                                                                                                                                                                                        |
| fidice     | class | `bot-pick`                                                                       |                                                                                                                                                                                                                 | `web/games/fidice/theme.css`     | The legacy lobby's strategy `<select>`; the typed lobby builds `bot-strategy`. Dead, removal is step 15's.                                                                                                                                                                                                     |
| shared     | class | `hidden`                                                                         | `web/shared/ui/shellPaint.ts` (`paintScreen`, `paintWaiting`, `paintHandoff`, `paintSheet`), `web/shared/ui/curtain.ts` (`paintCurtain`), `web/shared/ui/home.ts` (`paintTabs`, `paintPlayMode`, `paintResume`) | `web/shared/styles/base.css`     | The show/hide convention, toggled for gin and backgammon by the shared shell painters since docs/design/shared-shell.md §5 B1 and by the home shell since B2 (the extraction sees the literal; the row says which module owns it). Fidice never toggles it (its vdom omits a node instead) but links the rule. |
| fidice     | class | `fixed-screen pulse`                                                             | `web/shared/ui/shellPaint.ts` (`paintScreen`, `paintWaiting`)                                                                                                                                                   |                                  | Named by the shared shell painters fidice does not boot (docs/design/shared-shell.md §4.6: it joins the shell with its restyle); its theme has no rule for either, gin's and backgammon's do.                                                                                                                  |
| fidice     | class | `force-open`                                                                     | `web/shared/ui/home.ts` (`paintSubmenu`)                                                                                                                                                                        |                                  | The shared home shell holds the Play tab's submenu open; gin and backgammon style it in their theme.css, and fidice's page does not compose the shell yet (docs/design/shared-shell.md §4.6), so no rule here until it joins.                                                                                  |
| fidice     | class | `in-range rolling`                                                               |                                                                                                                                                                                                                 | `web/games/fidice/theme.css`     | `.lrow.in-range`, `.die.rolling`: nothing sets either since the port. Dead, removal is step 15's.                                                                                                                                                                                                              |
| fidice     | class | `kbd ladder-wrap or scene`                                                       |                                                                                                                                                                                                                 | `web/games/fidice/theme.css`     | Rules for markup the legacy page had and the typed screens do not build. Dead, removal is step 15's.                                                                                                                                                                                                           |
| backgammon | class | `checker ck-light ck-dark top`                                                   | `web/games/backgammon/src/ui/board.ts` (`checkersHtml`)                                                                                                                                                         | `web/games/backgammon/theme.css` | Template per checker: `checker ${owner} ${top}` with `style="--i:N"`; the top visible one carries `top` and, past five, `data-count` (design §2.2, §3.5).                                                                                                                                                      |
| backgammon | class | `slab`                                                                           | `web/games/backgammon/src/ui/board.ts` (`slabsHtml`)                                                                                                                                                            | `web/games/backgammon/theme.css` | One per borne-off checker in `#offLight`/`#offDark`; a flattened face laid from the tray's outer edge.                                                                                                                                                                                                         |
| backgammon | class | `die die-1 die-2 die-3 die-4 die-5 die-6 blank used dead picked theirs`          | `web/games/backgammon/src/ui/board.ts` (`diceHtml`)                                                                                                                                                             | `web/games/backgammon/theme.css` | Template `die die-${face}` plus the state words; the pips are `::before`/`::after` box-shadow dots, `blank` the unrolled face (design §3.7).                                                                                                                                                                   |
| backgammon | class | `chip faces via hits`                                                            | `web/games/backgammon/src/ui/board.ts` (`chipsHtml`)                                                                                                                                                            | `web/games/backgammon/theme.css` | The die-chip tray's buttons (`data-dice data-to data-via data-hit`); `faces`/`via` are their two lines, `hits` colours the via line (design §4.3).                                                                                                                                                             |
| backgammon | class | `flyer flyer-slab`                                                               | `web/games/backgammon/src/ui/board/fly.ts` (`addClass`)                                                                                                                                                         | `web/games/backgammon/theme.css` | The flight clone fixed over the page; `flyer-slab` while it flattens toward a tray (design §3.9).                                                                                                                                                                                                              |
| backgammon | class | `drag-ghost landing`                                                             | `web/games/backgammon/src/ui/board/dragger.ts`                                                                                                                                                                  | `web/games/backgammon/theme.css` | Gin's dragger reshaped: the ghost that follows the pointer and its glide back (design §4.12).                                                                                                                                                                                                                  |
| backgammon | class | `can-move selected auto target target-2 only hit shake arriving drop`            | `web/games/backgammon/src/ui/render.ts` (`paintHighlights`, `paintDrag`)                                                                                                                                        | `web/games/backgammon/theme.css` | Toggled on `.point`/`.bar`/`.off` (and `arriving` on a checker) outside the `data-key`, so a selection never rebuilds the stack (design §3.7).                                                                                                                                                                 |
| backgammon | class | `pt-near pt-far`                                                                 | `web/games/backgammon/src/ui/render.ts` (`paintSeat`)                                                                                                                                                           | `web/games/backgammon/theme.css` | The markup ships seat 0's; `paintSeat` rewrites them with `data-own` on a seat swap (own 1..12 near).                                                                                                                                                                                                          |
| backgammon | class | `inert rolling choosing to-move`                                                 | `web/games/backgammon/src/ui/render.ts`                                                                                                                                                                         | `web/games/backgammon/theme.css` | `inert` on `#board`, `rolling` on `#dice`, `choosing` on `#controls`, `to-move` on the opponent's strip (design §3.2, §3.7).                                                                                                                                                                                   |
| backgammon | class | `pulse muted centered left`                                                      | `web/shared/ui/shellPaint.ts` (`paintWaiting`)                                                                                                                                                                  | `web/games/backgammon/theme.css` | Markup literals the painters also toggle (`pulse` on `#hostWaitStatus`, by the shared shell since docs/design/shared-shell.md §5 B1); listed so the row cannot go stale either way.                                                                                                                            |
| backgammon | class | `hit`                                                                            | `web/games/backgammon/src/ui/render.ts` (`toastMarks`, through `web/shared/ui/shellPaint.ts` `showToast`)                                                                                                       | `web/games/backgammon/theme.css` | Also on `#toast` for the "Kapará." hit toast (one warm edge): this game's mark, handed to the shared toast as a record the extraction cannot see; the same word marks a hit point above.                                                                                                                       |
| backgammon | class | `score-row history-row who`                                                      | `web/games/backgammon/src/ui/render.ts` (`scoreHtml`, `historyHtml`)                                                                                                                                            | `web/games/backgammon/theme.css` | The rows of `#matchScore` and `#historyList`, built once per result key (design §2.2).                                                                                                                                                                                                                         |
| backgammon | class | `jargon rule-flash`                                                              | `web/shared/ui/glossary.ts` (`JARGON_CLASS`), `web/shared/edge/glossary.ts` (`RULE_FLASH_CLASS`)                                                                                                                | `web/games/backgammon/theme.css` | Gin's row; the same shared helpers write the links and the flash on this page (fidice has neither yet: its rules screen carries no ids).                                                                                                                                                                       |
| backgammon | class | `on off`                                                                         | `web/games/backgammon/src/ui/render.ts` (`connDotClass`)                                                                                                                                                        | `web/games/backgammon/theme.css` | `#oppDot`'s whole class attribute is a template written with `setAttr` (gin's rule); `off` is also the trays' markup class, which has its own rule.                                                                                                                                                            |

Rules of the table:

- `Owner` is `gin-rummy`, `fidice`, `backgammon` or `shared` (a `shared` row applies to every game).
- `Kind` is `class`; `id` and `attr` rows are not checked yet.
- `Name` is one class or several separated by spaces (`m0 m1 m2 m3 m4`).
- `Toggled by` names the TS module that produces the name; `Styled in` the stylesheet with the rule.
- Both filled: TS builds the name in a way the extraction cannot see, and the rule must exist.
- `Styled in` empty: a behaviour-only hook (no rule may exist for it, or the row is stale).
- `Toggled by` empty: markup-only or dead CSS (nothing in TS may name it, or the row is stale).

## Tokens

The shared vocabulary (docs/MIGRATION.md step 14 Deviations) is gin's palette: the roadmap
restyles Fidice to look like Gin, so `web/shared/styles/tokens.css` declares the eleven names below
with gin's values and gin's `theme.css` declares none of them (`test/tokens.test.ts` holds both).
Fidice's `theme.css`, linked last, redeclares every one of them so the names resolve to its own
palette until the restyle: the seven it always declared under the same name keep their values, and
the four it lacked are aliased onto its role equivalents. Backgammon's `theme.css`
(docs/design/backgammon-board.md §3) redeclares all eleven too, on purpose and for good: its
parchment-and-aegean palette is its own, not a restyle target, and its rules read the shared names
directly (`--felt` is the olive-wood board). The computed-style goldens pin every declared custom
property and its `:root` value; a new shared name is a note in
`tools/parity/computed-styles.ts --check` until the goldens are re-recorded, a changed value a
difference. `test/tokens.test.ts` pins that gin declares none of the names and that fidice and
backgammon declare every one. Columns are five on purpose: `test/dist/classes.ts` reads the
six-column class table above and nothing else.

| Shared name     | Canonical value (tokens.css, gin)                                            | Fidice override (theme.css)  | Backgammon override (theme.css)                                  | Notes                                                                                                                            |
| --------------- | ---------------------------------------------------------------------------- | ---------------------------- | ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `--bg`          | `#0f2318`                                                                    | `var(--mist)` (`#dcecf5`)    | `#e7d7be`                                                        | Alias in fidice (`--mist` is the top stop of its sky gradient); in backgammon the parchment tile's rendered mean (design §3.11). |
| `--card`        | `#163526`                                                                    | `var(--birch)` (`#fbf7ef`)   | `#efe8dc`                                                        | Dark panel vs. light birch card vs. the light checker's body.                                                                    |
| `--card-2`      | `#1c4030`                                                                    | `var(--cream-2)` (`#ece1cc`) | `#e4dac4`                                                        | Alias in fidice. Secondary surfaces (`.btn-secondary`, inputs).                                                                  |
| `--accent`      | `#4ade80`                                                                    | `#d9782f`                    | `#0b3c5d`                                                        | Green vs. orange vs. aegean blue, backgammon's one accent.                                                                       |
| `--accent-dark` | `#22c55e`                                                                    | `#a9561d`                    | `#06253d`                                                        | Backgammon: pressed buttons, the dark checker's body, the curtain wash.                                                          |
| `--gold`        | `#fbbf24`                                                                    | `#e6a93c`                    | `#c9a227`                                                        | Backgammon: hairlines only (the meander, the light checker's ring, the selected ring).                                           |
| `--text`        | `#f2fdf6`                                                                    | `var(--ink)` (`#22302a`)     | `#2a1a12`                                                        | Alias in fidice. Light on dark vs. dark on light; fidice rules still read `--ink`.                                               |
| `--muted`       | `#9cc9ac`                                                                    | `#66746c`                    | `#685745`                                                        | Backgammon: point labels, secondary text, disabled; 4.9:1 on the parchment (4.5:1 at its darkest percentile).                    |
| `--danger`      | `#f87171`                                                                    | `var(--red)` (`#b8433a`)     | `#b7472a`                                                        | Alias in fidice (rules still read `--red`). Backgammon: the hit flash and the hit toast's edge.                                  |
| `--radius`      | `18px`                                                                       | `16px`                       | `12px`                                                           |                                                                                                                                  |
| `--felt`        | `radial-gradient(ellipse at 50% 30%, #1f5a3a 0%, #123a26 55%, #0b2418 100%)` | `#1f4d3a`                    | `linear-gradient(160deg, #a67c52 0%, #8e6740 55%, #6e4b2a 100%)` | Gin paints the body with it; fidice a flat pine green; backgammon the olive-wood board field.                                    |

No fidice rule reads the four aliases yet (`--bg --card-2 --text --danger`): they exist so the
vocabulary resolves everywhere, and the restyle is what points fidice's rules at the shared names.
Not tokens in either game: gin's `.card-box` shadow is inline (`0 4px 14px rgba(0,0,0,0.25)`) while
fidice has `--shadow: 0 8px 26px rgba(21,54,39,.16)`; neither names its body font stack
(`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif` vs.
`'Nunito', system-ui, sans-serif`).

Game-only tokens, unchanged and not shared: gin `--card-w --mini-w --pile-w --tiny-w` (the card
sizes; `#tableScreen` redeclares `--card-w` and `--pile-w` as clamp()s of the viewport, per element
`.card.mini`/`.big`/`.back.tiny` alias `--card-w` onto the other three), still in its `theme.css`;
fidice `--felt-dark --felt-light --pine --pine-light --moss --lake --lake-deep
--lake-light --mist --wood --wood-dark --wood-light --timber --cream --cream-2 --birch --ink --red
--blue --line --shadow`; backgammon `--olivewood-dark --stone --olive --olive-leaf --nazar
--nazar-pale --walnut --nacre-sheen --panel --nacre --nacre-dim --hair --parchment-cream --serif-display --serif-text --checker-d --die-s` on `:root`
(only the names its rules read) and the board geometry on `#tableScreen` (`--chrome-h --bar-w
--off-h --off-w --point-w --point-len --checker-d --stack-step --die-s`, clamp()s of the viewport
like gin's `--card-w`; docs/design/backgammon-board.md §3).
