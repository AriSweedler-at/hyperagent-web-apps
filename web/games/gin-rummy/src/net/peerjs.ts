// The shared peer plumbing under its gin name: eslint's EDGES glob and the sessions beside it name
// `net/peerjs.ts`, and the body lives in web/shared/edge/peer.ts since it serves every game.
export * from '../../../../shared/edge/peer.ts';
