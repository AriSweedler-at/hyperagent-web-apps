// GENERATED by tools/legacy/debundle-fidice.ts from legacy/fidice/index.html lines 551-556
// (bundle section "// src/domain/result.ts"): the body is verbatim, indentation included; the imports
// and exports were recovered by scope analysis. Do not edit: `npm run debundle:fidice` regenerates
// it and test/tools/debundle-fidice.test.ts checks it (docs/MIGRATION.md step 6; step 8 types it).

  var ok = (value) => ({ ok: true, value });
  var err = (error) => ({ ok: false, error });
  var expect = (r, context = "result") => {
    if (r.ok) return r.value;
    throw new Error(`${context}: ${String(r.error)}`);
  };

export { ok, err, expect };
